from  setuptools import setup

setup(
    name='startifiedRS',
    version='0.0.1',
    descripiton='select a startified_random_smaple from a population dataframe with sampe proportions of sample elments for each of starata of the population',
    py_modules=["startifiedRS"],
    package_dir={'': 'src'},
)
